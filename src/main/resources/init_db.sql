CREATE TABLE `library_db`.`books` (
                                      `id` INT NOT NULL AUTO_INCREMENT,
                                      `title` VARCHAR(45) NULL,
                                      `price` BIGINT NULL,
                                      PRIMARY KEY (`id`));
