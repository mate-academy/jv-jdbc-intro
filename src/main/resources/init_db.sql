CREATE TABLE book (
                           id INT NOT NULL AUTO_INCREMENT,
                           title VARCHAR(50),
                           price DECIMAL(10, 4),
                           PRIMARY KEY(id)
);