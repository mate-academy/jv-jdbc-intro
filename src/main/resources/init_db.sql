CREATE DATABASE library;
USE library;
CREATE TABLE books (
    id BIGINT PRIMARY KEY,
    title VARCHAR(255),
    price BIGINT(45)
);
ALTER TABLE books MODIFY COLUMN
    id BIGINT NOT NULL AUTO_INCREMENT;
ALTER TABLE books
    ALTER id SET DEFAULT 0;