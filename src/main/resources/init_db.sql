CREATE TABLE books (
                       id bigint NOT NULL AUTO_INCREMENT,
                       title varchar(255) NOT NULL,
                       price decimal NOT NULL,
                       PRIMARY KEY (id)
);