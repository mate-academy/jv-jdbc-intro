CREATE SCHEMA IF NOT EXISTS home_work;

USE home_work;

CREATE TABLE IF NOT EXISTS books(
    id BIGINT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL ,
    price DECIMAL(5,2) NOT NULL
);