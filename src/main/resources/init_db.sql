CREATE TABLE `books` (
                         id INT NOT NULL AUTO_INCREMENT,
                         title VARCHAR(255) NOT NULL,
                         price INT NOT NULL,
                         PRIMARY KEY (id)
);
